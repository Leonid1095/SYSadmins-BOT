# Пример переменных окружения. Скопируйте в .env или задайте в systemd.
# Значения также можно (и по умолчанию — проще) хранить прямо в config.py.

# Токен бота от @BotFather. ОБЯЗАТЕЛЬНО новый (старый скомпрометирован).
TELEGRAM_TOKEN=

# Telegram user id владельца — получает локальные алерты monitor.sh
OWNER_ID=1148520376
